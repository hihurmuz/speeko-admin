# Speeko Admin — Environment Variables
# Kopyala: cp .env.example .env

# API base URL (speeko-api)
VITE_API_BASE_URL=http://localhost:8000

# Admin secret (basic auth veya JWT doğrulama için)
VITE_ADMIN_SECRET=changeme
